.row
  .col-sm-12
    .container-flex
      div(offering-priceplan, ng-repeat="priceplan in detailVM.item.productOfferingPrice")
  .col-sm-12(ng-if="!detailVM.item.productOfferingPrice.length")
    .alert.alert-info.text-center 没有价格计划