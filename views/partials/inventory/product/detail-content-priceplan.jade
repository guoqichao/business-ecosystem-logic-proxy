.row
  .col-xs-12
    .container-flex
      div(offering-priceplan, ng-repeat="priceplan in detailVM.item.productOffering.productOfferingPrice")
    .alert.alert-info.text-center(ng-if="!detailVM.hasProductPrice()") 没有选中的价格计划 (免费商品).