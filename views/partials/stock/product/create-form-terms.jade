form(name="step.form", novalidate)
  .row
    .col-sm-12
      .form-group(
        ng-class="{ 'has-error': createForm.hasError(step.form.name) }"
      )
        label
          span 条款标题
          //gqc Enter agreement title
          small.text-muted (可选)
        input.form-control(
          type="text"
          name="title"
          ng-model="createVM.terms.title"
          ng-maxlength="50"
        )
        .help-block(
          ng-messages="step.form.name.$error"
          ng-show="createForm.hasError(step.form.name)"
        )
          span(ng-message="maxlength") 字段不能超过 50 个字符.
  .form-group
    label
      span 条款内容
      //gqc Enter agreement text
      small.text-muted (可选)
    textarea.form-control(rows="6", name="text", ng-model="createVM.terms.text")
  .form-group.text-right
    a.btn.btn-default.z-depth-1(ng-click="createForm.nextStep($index + 1, createVM.stepList[$index + 1])", ng-disabled="!step.form.$valid")
      span.item-text 下一步